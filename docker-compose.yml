version: "3.9"

services:
<<<<<<< HEAD
  web:
    build:
      context: .
      target: runtime       # usa solo la imagen final, no el builder
    image: travelpath:local
    container_name: travelpath_web
    ports:
      - "5000:5000"
    env_file:
      - .env                # carga DATABASE_URL, JWT_SECRET_KEY, etc.
    environment:
      - FLASK_DEBUG=false   # siempre false en el contenedor, aunque .env diga true
=======

  airport-service:
    build:
      context: ./airport-service
    image: travelpath-airport:local
    container_name: travelpath_airport
    ports:
      - "8001:8001"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  itinerary-service:
    build:
      context: .
      target: runtime
    image: travelpath-itinerary:local
    container_name: travelpath_itinerary
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=false
      - AIRPORT_SERVICE_URL=http://airport-service:8001
    depends_on:
      airport-service:
        condition: service_healthy
>>>>>>> origin/master
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:5000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
